class Ship {   
    constructor(size, direction, startX, startY ) {
        this.size = size
        this.maxSize = size
        this.direction = direction
        this.startX = startX
        this.startY = startY
    }
} 

module.exports = Ship